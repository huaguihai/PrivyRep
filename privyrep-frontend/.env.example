# PrivyRep Frontend Environment Variables

# Contract Version Selection
# Set to 'true' to use V2 (Oracle callback), 'false' to use V1 (manual completion)
# Default: false (V1 mode for reliable demo)
VITE_USE_V2=false

# Note: V2 requires Zama Relayer to be online
# Check Relayer status: cd ../privyrep-contracts && npm run check-relayer
